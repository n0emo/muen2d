#pragma once

using JSRuntime = struct JSRuntime;
using JSContext = struct JSContext;
using JSValue = struct JSValue;

using Texture = struct Texture;
using Sound = struct Sound;
using Music = struct Music;

using zip_t = struct zip;

namespace muen {

class Game;
class Engine;

} // namespace muen
